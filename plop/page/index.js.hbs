import Head from "next/head";

import styles from "@/styles/{{path}}/{{kebabCase name}}.module.css";

export default function {{pascalCase name}}() {
  return (
    <div className={styles.container}>
      <Head>
        <title>Themis App - {{pascalCase name}}</title>
      </Head>

      <h1>{{sentenceCase name}}</h1>
    </div>
  );
}

export async function getServerSideProps(context) {
  return {
    props: {},
  };
}

